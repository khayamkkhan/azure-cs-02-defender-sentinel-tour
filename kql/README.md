# KQL Query Collection — Project 02

Curated KQL queries used to verify the Sentinel pipeline and explore Azure Activity / Entra ID telemetry. Built during the SC-900 lab to confirm "the telemetry is there" (detections come in Project 05 / SC-200).

| File | Source Table(s) | Purpose |
| ---- | --------------- | ------- |
| _(queries will be added as the lab progresses)_ | | |

---

## Convention

Every `.kql` file includes:

1. A `// ===` header block: **purpose**, **author**, **date**, **source tables**, **expected result count**
2. The query body with line-by-line comments for any non-obvious operator
3. A `// Example output` block at the end showing a sanitised sample row

This makes the queries self-documenting for future-me and recruiters reading the repo.
